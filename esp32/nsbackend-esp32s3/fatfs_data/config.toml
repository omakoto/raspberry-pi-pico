# nsbackend-esp32s3 Configuration
hostname = "nscon"
tcp_port = 10100
log = true
enable_echo = true
led_active_low = true

# Primary Wi-Fi Access Point (Index 0)
wifi_ssid = "YOUR_WIFI_SSID"
wifi_password = "YOUR_WIFI_PASSWORD"

# Fallback Wi-Fi Access Points (Optional: indices 1 to 9)
# wifi_ssid1 = "BackupHotspot"
# wifi_password1 = "HotspotPass"

# I2C Matrix Keypad (PCF8574 / PCF8574A)
# Uses standard I2C pins: SDA on GPIO 5 (D4 on XIAO), SCL on GPIO 6 (D5 on XIAO)
i2c_keypad_enabled = true
i2c_sda_pin = 5
i2c_scl_pin = 6
i2c_address = 0x20
i2c_reverse_row = true
i2c_reverse_col = true
i2c_debounce_ms = 20

